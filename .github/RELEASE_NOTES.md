## What's new

- New **Enhance** panel — denoise, sharpen / detail, local contrast (CLAHE), vignette
- Five new one-click filters — oil paint, detail enhance, posterize, film grain, dehaze
- **Perspective correction** — click four corners to straighten a photo
- **Batch processing** now also applies filters and enhancement
- **macOS trackpad fixed** — two-finger scroll pans, pinch zooms (previously every scroll zoomed)

> This section describes the current release. Update it (and the Chinese copy below) before publishing a new version.

## Download

Pick the file for your platform — **no Python installation required**.

| Platform | File |
|---|---|
| **Windows** | `…-windows-setup.exe` (installer) or `…-windows-portable.zip` |
| **macOS (Apple Silicon)** | `…-macos-arm64.dmg` |
| **macOS (Intel)** | `…-macos-x86_64.dmg` |

macOS builds are also available as `.zip` archives.

## First launch

The app isn't code-signed, so the OS warns you once — this is expected:

- **Windows**: *"Windows protected your PC"* → **More info** → **Run anyway**
- **macOS**: *"the developer cannot be verified"* → **right-click the app → Open** → **Open** again

You only need to do this once.

---

<details>
<summary><strong>中文说明（点击展开）</strong></summary>

## 本次更新

- 新增**「增强」面板**：降噪、锐化细节、局部对比度（CLAHE）、暗角
- 新增 5 个滤镜：油画、细节增强、色调分离、颗粒、去雾
- 几何页新增**四点透视矫正**，点四个角即可把画面拉正
- **批量处理**现在也支持滤镜与画质增强
- **修复 macOS 触控板**：双指滚动改为平移、捏合缩放（此前一滚动就缩放）

> 本节描述的是当前版本，发布新版本前记得连同上面的英文一起更新。

## 下载

按平台选对应文件即可，**不需要安装 Python**。

| 平台 | 文件 |
|---|---|
| **Windows** | `…-windows-setup.exe`（安装包）或 `…-windows-portable.zip`（便携版） |
| **macOS（M 系列）** | `…-macos-arm64.dmg` |
| **macOS（Intel）** | `…-macos-x86_64.dmg` |

macOS 版另有 `.zip` 压缩包可选。

## 首次打开

程序没有商业代码签名，第一次打开系统会拦一次，属正常现象：

- **Windows**：「Windows 已保护你的电脑」→「**更多信息**」→「**仍要运行**」
- **macOS**：「无法验证开发者」→ 图标上**右键 → 打开** → 再点一次「**打开**」

只需要操作一次。

</details>

---

功能介绍见 [README](https://github.com/evan-rio/image-tool#readme)，许可见 [LICENSE](https://github.com/evan-rio/image-tool/blob/main/LICENSE)。
